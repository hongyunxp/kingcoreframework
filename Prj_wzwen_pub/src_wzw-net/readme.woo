*wuzewen on 2005-7-31
这是一个纯技术的包，不属于任何一个子系统，任何子系统可以使用这个包。
wzw-net 包中除了DBUtils.class、WebPageUtils与wpub.jar关联外，其他都是无依赖的。

>为什么需要在第三方调用上封装一层？
	便于灵活修改自己的代码，同时为了适应第三方的变化，
	如果代码中大量直接使用第三方而无封装，那么第三方变动导致的系统修改量将很大。

	